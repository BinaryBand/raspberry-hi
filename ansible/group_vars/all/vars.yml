---
ansible_python_interpreter: /usr/bin/python3

# Homebrew — single source of truth for brew_user and prefix
brew_user: erron
brew_prefix: /home/linuxbrew/.linuxbrew

# Shared MinIO/storage config — used by both the storage and minio roles
minio_data_path: /srv/minio/data

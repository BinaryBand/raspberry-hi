# Local secrets — never commit
.bw-session
.vault-password
group_vars/all/vault.yml

# Generated by 'make generate-apps' — do not commit
apps/*/defaults/generated.yml
group_vars/all/vars.yml

# Project-local configs (must never be committed)
config/

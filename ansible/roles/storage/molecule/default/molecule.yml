---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: debian-test
    image: geerlingguy/docker-debian12-ansible:latest
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible

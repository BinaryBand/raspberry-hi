---
- name: Ensure MinIO data directory exists
  ansible.builtin.file:
    path: "{{ minio_data_path }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0750"

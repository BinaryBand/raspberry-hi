---
- name: Ensure MinIO data directory exists
  ansible.builtin.file:
    path: "{{ minio_data_path }}"
    state: directory
    owner: "{{ brew_user }}"
    group: "{{ brew_user }}"
    mode: "0750"

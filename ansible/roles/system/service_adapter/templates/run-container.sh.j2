#!/bin/sh
# Auto-generated by service_adapter — do not edit manually.
# Starts {{ svc_name }} via Podman. Used by cron and manual backends.
{% if svc_requires_mount is defined and svc_requires_mount %}
# Wait for required mount before starting.
until mountpoint -q {{ svc_requires_mount }}; do
  sleep 2
done
{% endif %}
podman start {{ svc_name }} 2>/dev/null || \
  podman run -d --name {{ svc_name }} {{ svc_run_args | default('') }}

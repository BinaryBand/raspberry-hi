# TYPE  DATABASE           USER               ADDRESS   METHOD

# Local socket — for container-internal administration and Ansible provisioning via podman exec
local   all                all                          trust

# Application users — each can only authenticate against their own database
host    baikal             baikal             all       scram-sha-256
host    synapse            synapse            all       scram-sha-256
host    mautrix_whatsapp   mautrix_whatsapp   all       scram-sha-256

# Reject everything else
host    all                all                all       reject

---
# Install Podman via apt on Debian/Ubuntu.
- name: Install Podman (apt)
  ansible.builtin.apt:
    name: podman
    state: present
    update_cache: true
    cache_valid_time: 3600
  when: ansible_facts['pkg_mgr'] == "apt"
